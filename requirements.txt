# Core dependencies. Versions are unpinned; the code targets Python 3.10+.
# torch with float64 default dtype is assumed throughout.
#
# IMPORTANT: the SSBroyden2 optimizer requires a PATCHED scipy. Replace
# scipy/optimize/_optimize.py with the version from
# https://github.com/EliKiani/Optimizing_the_Optimizer_PINNs  (see README).
numpy
scipy
torch
matplotlib
tqdm

# Mesh generation for the cylinder benchmark (cylinder/cylinder_mesh.py).
gmsh

# Result aggregation for the ablation sweeps (common/sweep.py).
pandas

# NOTE: the SOAP optimizer is NOT a dependency -- it is vendored in common/soap.py
# from https://github.com/nikhilvyas/SOAP (MIT), so the optimizer used in the paper
# is pinned and identical on every node. See that file for the two modifications
# (float64 preservation, weight_decay default 0).
